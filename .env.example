# Backend served at http://127.0.0.1:8000. The Vite dev server proxies /api here.
SMARTSET_HOST=127.0.0.1
SMARTSET_PORT=8000

# Everything the live call and the Anthropic calls need lives in the untracked
# retention_engine/.env. See retention_engine/.env.example.
